if [[ -d $HOME/anaconda ]]; then
  CONDAPATH="$HOME/anaconda/bin:"
else
  CONDAPATH=""
fi

if [[ -d $HOME/homebrew ]]; then
  BREWPATH="$HOME/homebrew/bin:"
else
  BREWPATH=""
fi

export PATH="${CONDAPATH}${BREWPATH}${PATH}:$HOME/bin:$HOME/.local/bin:/usr/local/go/bin:$GOPATH/bin"
